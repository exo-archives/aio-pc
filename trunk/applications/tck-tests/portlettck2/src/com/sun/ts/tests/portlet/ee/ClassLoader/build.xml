<!DOCTYPE project [
  <!ENTITY Common SYSTEM "file:../../common/subProject.xml">
]>
<project name="portlet_jp_ClassLoader" basedir="." default="usage" >

    <!-- Properties to be defined before including Common -->
	<property name="app.name"  value="portlet_jp_ClassLoader" />
	<property name="pkg.name"   value="ee/ClassLoader" />

    <!-- import common defs -->
    &Common;
</project>
